<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Toy Trader</title>
    <!-- boostrap -->
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <!-- main css -->
    <link rel="stylesheet" href="/css/style.css" />
    <!-- font awesome -->
    <script src="/js/all.js"></script>
  </head>

  <body>
    <!-- navbar -->
    <nav
      class="navbar navbar-expand-md navbar-dark bg-dark fixed-top text-capitalize"
    >
      <a class="navbar-brand" href="/">Toy Trader</a>
      <div class="d-flex justify-content-end">
        <%if(tkdalogin){%>

        <a class="nav-link disabled" href="/#"
          >Hello <%= tkdalogin.username %></a
        >

        <a class="nav-link" href="/logoutT">Logout </a>

        <%}else{%>

        <a class="nav-link" href="/loginT">Login </a>

        <a class="nav-link" href="/registerT">Sign Up </a>

        <%}%>
        <button
          class="navbar-toggler"
          type="button"
          data-toggle="collapse"
          data-target="#navbarSupportedContent"
          aria-controls="navbarSupportedContent"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
      </div>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item">
            <a class="nav-link" href="/postT">Post </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/browseT">Browse </a>
          </li>
        </ul>
      </div>
    </nav>
    <div class="flashmessage">
      <% if(error && error.length >0 ) {%>
      <div class="bg-danger flash"><%=error%></div>
      <%}else if(success && success.length >0){%>
      <div class="bg-success flash"><%=success%></div>
      <%}%>
    </div>
  </body>
</html>
